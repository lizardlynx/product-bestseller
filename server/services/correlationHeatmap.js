class CorrelationHeatmap {
  get() {
    // get avg price for all shops
    // get other data

    // for each of data, get correlation coefficient with products data
    // build matrix -> return it.
  }
}

module.exports = new CorrelationHeatmap();
